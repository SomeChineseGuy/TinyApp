<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
<header> <% include partials/_header %> </header>
 <h1> My links. </h1>
    <% for (var urlsid of urlArray) { %>
    <li>
      <a href="/urls/<%= urlsid.short %>"><%= urlsid.short %></a>  -----------> <a href ="<%= urlsid.long %>">  <%= urlsid.long %> </a>
      <form method="POST" action="/urls/<%= urlsid.short %>/delete">
        <button type="submit">Delete</button>
      </form>
      <form method="GET" action="/urls/<%= urlsid.short %>">
      <button type="submit">Update</button></form>
      </li>
    <% } %>
    <p> Add new shorten URL <a href="/urls/new">here!</a>
</body>
</html>